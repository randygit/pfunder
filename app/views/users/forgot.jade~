extends ../layouts/layout

block head
  title Forgot you password
block neck
    link(rel='stylesheet', href='/css/contact.css') 
    link(rel='stylesheet', href='/css/signup.css') 

block feet

block content
  div.row
    div.col-sm-6
      div.messages
        br
        br 
        .alert.alert-warning
          strong= error
      div.page-header
        h1 Forgot your password?
      .container
        form(name="form", method="post", action="/forgot", novalidate)
          div.form-group
            label.control-label(for="email") Enter Your Email:
            input.form-control(type="email", ng-model="email", name="email", id="email", placeholder="Enter your Email", required )
            span.help-block(ng-show="form.email.$dirty && form.email.$error.email") Email is invalid
            span.help-block(ng-show="form.email.$dirty && form.email.$error.isTaken") Email is already used
            span.help-block(ng-show="form.email.$dirty && form.email.$error.required") Please enter email
          if error
            div.alert #{error}
          input.btn.btn-primary(type="submit", value="Submit", ng-disabled="form.$invalid")
          | 
          a.btn.btn-link(href='/login') Back to Login
          input(type='hidden', name='returnUrl', value=returnUrl)
            
    

