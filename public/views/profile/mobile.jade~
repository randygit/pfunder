
section(data-ng-controller="MobileFormController", data-ng-init="getDefault()") 

  div.col-sm-8.special 
    div.container 
      div.row
        br
        br
        div.panel.panel-default
          div.panel-body
            h3 Mobile
            .container
              form.form-horizontal(name="form", method="post",  novalidate)
                input(type='hidden', name = '_csrf', value = token)

                div.form-group
                  label.col-sm-2.control-label(for="country") Country
                  .col-sm-6
                  //<pre>Selected: {{mobile.country|json}}</pre>
                  select(style="width:400px", name="states2", id="states2", ui-select2='ui-select2', ng-model="mobile.country", data-placeholder="Choose country")
                    option
                      option(ng-repeat="country in countryData", value="{{country.code}}") {{country.name}}

                div.form-group
                  label.col-sm-2.control-label(for="number") Phone Number:
                  .col-sm-6
                  // <pre>Selected: {{mobile.number|json}}</pre>
                  input(style="width:400px", name="number", id="number",  ng-model="mobile.number", data-placeholder="Enter mobile phone number")
                     

                div.form-group
                  label.col-sm-2.control-label(for="carrier") Carrier:
                  .col-sm-6
                  <pre>Selected: {{mobile.carrier|json}}</pre>
                  select(style="width:400px", name = "carrier", ui-select2='ui-select2', ng-model='mobile.carrier', data-placeholder='Pick a Carrier')
                      option(value='')
                          option(value='globe') Globe
                          option(value='smart') Smart
                          option(value='sun') Sun

           

                // submit here
              input.btn.btn-primary(type="submit", value="Activate Phone", data-ng-click="updateMobile()")
       

